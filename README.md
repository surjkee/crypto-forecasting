# 📈 Crypto Real-Time Forecasting System

### Intelligent Cryptocurrency Analysis & LSTM-Based Price Prediction

------------------------------------------------------------------------

## 🧠 Overview

Це інтерактивна система для аналізу та прогнозування цін криптовалют у
режимі **t+1 (погодинний прогноз)** за допомогою нейронних мереж
**LSTM**, з подальшою візуалізацією в **Streamlit**.

Система автоматично: - збирає дані з API CoinGecko\
- зберігає їх у DuckDB\
- формує технічні фічі\
- тренує LSTM для кожної монети\
- робить прогноз **на наступну годину**\
- зберігає прогноз у БД\
- надає інтерфейс для перегляду фактів та прогнозів

------------------------------------------------------------------------

# 🏗 Структура проєкту

    crypto-forecasting/
    │
    ├── data/                  # DuckDB, кеш оригінальних даних та прогнозів
    ├── ingest/                # API-клієнти (CoinGecko)
    ├── features/              # Побудова фіч (SMA, volatility, returns)
    ├── models/                # LSTM, архітектури, тренування та inference
    ├── jobs/                  # Скрипти тренування та t+1 прогнозу
    ├── ui/                    # Streamlit-додаток
    └── config/                # Налаштування, ENV, конфіги моделей

------------------------------------------------------------------------

# ⚙️ Встановлення та запуск

## 1️⃣ Клонування репозиторію

``` bash
git clone https://github.com/username/crypto-forecasting.git
cd crypto-forecasting
```

------------------------------------------------------------------------

## 2️⃣ Створення віртуального середовища

### Windows PowerShell

``` bash
python -m venv .venv
.venv\Scripts\activate
```

### Linux / Mac

``` bash
python3 -m venv .venv
source .venv/bin/activate
```

------------------------------------------------------------------------

## 3️⃣ Встановлення залежностей

``` bash
pip install -r requirements.txt
```

> **УВАГА:**\
> За замовчуванням встановлюється CPU-версія PyTorch.\
> Для GPU CUDA --- дивись наступний розділ.

------------------------------------------------------------------------

# ⚡ Підтримка CUDA (NVIDIA GPU)

## CUDA 12.1 (рекомендовано)

``` bash
pip uninstall -y torch torchvision torchaudio
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
```

Перевірка:

``` bash
python -c "import torch; print(torch.cuda.is_available(), torch.cuda.get_device_name(0))"
```

------------------------------------------------------------------------

# 🔑 Налаштування API CoinGecko

Створіть файл `.env`:

    COINGECKO_API_KEY=CG-xxxxxxxxxxxxxxxxxxxx
    DEFAULT_VS_CURRENCY=usd
    HISTORY_DAYS_DEFAULT=60
    HISTORY_INTERVAL=hourly

## Відповідно до правил CoinGecko:

> **This project uses the CoinGecko API. This product uses the CoinGecko
> API but is not endorsed or certified by CoinGecko.\
> https://www.coingecko.com/**

![CoinGecko
Logo](https://static.coingecko.com/s/coingecko-logo-gradient-2e76f1d9a741b93d7c5edda6af7d2a3dfcc5a2dd.png)

------------------------------------------------------------------------

# 🐍 Порядок запуску

## 1️⃣ Завантаження історії

``` bash
python -m jobs.fetch_history
```

## 2️⃣ Тренування LSTM для всіх монет

``` bash
python -m jobs.train_lstm_all
```

## 3️⃣ Прогноз t+1

``` bash
python -m jobs.run_forecast
```

Прогнози зберігаються у таблиці `forecast_hourly`.

------------------------------------------------------------------------

# 🖥 Запуск Streamlit

``` bash
streamlit run ui/app.py
```

------------------------------------------------------------------------

# 📦 DuckDB Таблиці

    ohlcv_hourly       — сирі дані
    forecast_hourly    — збережені прогнози t+1

------------------------------------------------------------------------

# ✨ Ліцензія

Дані CoinGecko підпадають під їхні правила використання API.

------------------------------------------------------------------------

# 🙌 Credits

-   CoinGecko API\
-   PyTorch\
-   Streamlit\
-   DuckDB\
-   Plotly
